#!/bin/bash
echo "全部容器端口映射:"
echo "容器名称 -> 外部端口:容器端口"
echo "--------------------------------"
docker ps -a --format "{{.Names}} {{.Ports}}" | while read name ports; do
    if [ -n "$ports" ]; then
        echo "$name -> $ports"
    else
        echo "$name -> (无端口映射)"
    fi
done
